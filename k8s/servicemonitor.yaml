apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: devops-python-api
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: devops-python-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
